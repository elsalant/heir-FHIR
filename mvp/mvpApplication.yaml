apiVersion: app.fybrik.io/v1alpha1
kind: FybrikApplication
metadata:
  name: mvp-notebook
  namespace: mvp
  labels:
    app: mvp-notebook
spec:
  selector: 
    workloadSelector: {} 
  appInfo:
    intent: mvp-demo
  data:
    - dataSetID: "mvp/diabetes-json"
      requirements:
        copy:
          required: true
        interface: 
          protocol: s3
          dataformat: csv
